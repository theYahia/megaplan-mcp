// Surface counts, shared by the stdio startup log and the HTTP /health endpoint.
// Keep in sync with the tool/prompt registrations in src/index.ts.
export const TOOL_COUNT = 18;
export const PROMPT_COUNT = 2;
